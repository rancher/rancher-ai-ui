diff --git a/cypress/e2e/tests/features/message-actions.spec.ts b/cypress/e2e/tests/features/message-actions.spec.ts
index d697a33..734a04d 100644
--- a/cypress/e2e/tests/features/message-actions.spec.ts
+++ b/cypress/e2e/tests/features/message-actions.spec.ts
@@ -23,7 +23,7 @@ describe('Feature: message-actions', () => {
     chat.sendMessage('Copy this message');
     chat.getMessage(3).isCompleted();
 
-    chat.getMessage(3).copyButton().click();
+    cy.get('[data-testid="rancher-ai-ui-chat-container"]').type('{ctrl}{shift}c');
 
     cy.get('@clipboardWrite').should('have.been.calledWith', 'The copy response text.');
 
@@ -106,12 +106,22 @@ describe('Feature: message-actions', () => {
     cy.enqueueLLMResponse({
       mcpTool: {
         name: 'createKubernetesResource',
-        args: [{ name: 'test-pod', kind: 'Pod', namespace: 'default', resource: { apiVersion: 'v1' } }]
+        args: {
+          name:      'test-pod',
+          kind:      'Pod',
+          namespace: 'default',
+          cluster:   'local',
+          resource:  { apiVersion: 'v1', kind: 'Pod', metadata: { name: 'test-pod', namespace: 'default' } }
+        }
       }
     });
     chat.sendMessage('Show pending confirmation');
 
-    chat.getMessage(3).confirmButton().should('be.visible');
+    const confirmationRequestMessage = chat.getMessage(3);
+
+    confirmationRequestMessage.scrollIntoView();
+    confirmationRequestMessage.containsText('Are you sure you want to proceed with this action?');
+    confirmationRequestMessage.confirmButton().should('be.visible');
 
     chat.getMessage(2).editButton().self().should('not.exist');
     chat.getMessage(2).resendButton().self().should('not.exist');

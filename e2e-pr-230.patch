diff --git a/cypress/e2e/po/chat.po.ts b/cypress/e2e/po/chat.po.ts
index 4b9dba6..b73c680 100644
--- a/cypress/e2e/po/chat.po.ts
+++ b/cypress/e2e/po/chat.po.ts
@@ -64,22 +64,36 @@ export default class ChatPo extends ComponentPo {
   openViaKeyboard() {
     const isMac = Cypress.platform === 'darwin';
 
-    if (isMac) {
-      cy.get('body').trigger('keydown', { metaKey: true, shiftKey: true, key: 'k', keyCode: 75 });
-    } else {
-      cy.get('body').trigger('keydown', { altKey: true, key: 'k', keyCode: 75 });
-    }
+    // Dispatch on window directly so shortcut handlers registered via window.addEventListener receive the event
+    cy.window().then((win) => {
+      if (isMac) {
+        win.dispatchEvent(new win.KeyboardEvent('keydown', {
+          metaKey: true, shiftKey: true, key: 'k', keyCode: 75, bubbles: true, cancelable: true,
+        }));
+      } else {
+        win.dispatchEvent(new win.KeyboardEvent('keydown', {
+          altKey: true, key: 'k', keyCode: 75, bubbles: true, cancelable: true,
+        }));
+      }
+    });
     this.isOpen();
   }
 
   closeViaKeyboard() {
     const isMac = Cypress.platform === 'darwin';
 
-    if (isMac) {
-      cy.get('body').trigger('keydown', { metaKey: true, shiftKey: true, key: 'k', keyCode: 75 });
-    } else {
-      cy.get('body').trigger('keydown', { altKey: true, key: 'k', keyCode: 75 });
-    }
+    // Dispatch on window directly so shortcut handlers registered via window.addEventListener receive the event
+    cy.window().then((win) => {
+      if (isMac) {
+        win.dispatchEvent(new win.KeyboardEvent('keydown', {
+          metaKey: true, shiftKey: true, key: 'k', keyCode: 75, bubbles: true, cancelable: true,
+        }));
+      } else {
+        win.dispatchEvent(new win.KeyboardEvent('keydown', {
+          altKey: true, key: 'k', keyCode: 75, bubbles: true, cancelable: true,
+        }));
+      }
+    });
     this.isClosed();
   }
 

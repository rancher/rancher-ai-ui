diff --git a/cypress/e2e/po/chat.po.ts b/cypress/e2e/po/chat.po.ts
index b408849..64baf43 100644
--- a/cypress/e2e/po/chat.po.ts
+++ b/cypress/e2e/po/chat.po.ts
@@ -62,19 +62,16 @@ export default class ChatPo extends ComponentPo {
   }
 
   openViaKeyboard() {
-    // The v-shortkey plugin disables its document keydown listener when NODE_ENV=test.
-    // Dispatch the 'shortkey' CustomEvent directly on the header button — same event
-    // the plugin would emit after decoding the keydown.
-    this.rancherHeader.askLizButton().then(($el) => {
-      $el[0].dispatchEvent(new CustomEvent('shortkey', { bubbles: false }));
-    });
+    const isMac = Cypress.platform === 'darwin';
+
+    cy.get('body').type(isMac ? '{meta}{shift}k' : '{alt}k');
     this.isOpen();
   }
 
   closeViaKeyboard() {
-    this.rancherHeader.askLizButton().then(($el) => {
-      $el[0].dispatchEvent(new CustomEvent('shortkey', { bubbles: false }));
-    });
+    const isMac = Cypress.platform === 'darwin';
+
+    cy.get('body').type(isMac ? '{meta}{shift}k' : '{alt}k');
     this.isClosed();
   }
 
diff --git a/cypress/e2e/tests/features/chat-open-shortcut.spec.ts b/cypress/e2e/tests/features/chat-open-shortcut.spec.ts
index 7f34ede..b88034a 100644
--- a/cypress/e2e/tests/features/chat-open-shortcut.spec.ts
+++ b/cypress/e2e/tests/features/chat-open-shortcut.spec.ts
@@ -95,11 +95,9 @@ describe('Feature: chat-open-shortcut', () => {
 
     cy.get('[data-testid="rancher-ai-ui-chat-input-textarea"]').click().type('partial message');
 
-    cy.get('[data-testid="rancher-ai-ui-chat-input-textarea"]').trigger('keydown', {
-      altKey:  true,
-      key:     'k',
-      keyCode: 75
-    });
+    const isMac = Cypress.platform === 'darwin';
+
+    cy.get('body').type(isMac ? '{meta}{shift}k' : '{alt}k');
 
     cy.get('[data-testid="rancher-ai-ui-chat-container"]').should('not.exist');
 
